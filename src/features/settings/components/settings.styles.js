import styled from "styled-components";
import { List } from "react-native-paper";

export const SettingsList = styled(List.Section)`
  padding: 16px;
`;

export const AvatarContainer = styled.View`
  margin: 16px;
  align-self: center;
  align-items: center;
`;
