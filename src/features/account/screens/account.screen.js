import { AccountBackground, AccountCover } from "../components/accounts.styles";

export const AccountScreen = () => {
  return (
    <AccountBackground>
      <AccountCover />
    </AccountBackground>
  );
};
